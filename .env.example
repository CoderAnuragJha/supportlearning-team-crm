# Database Configuration
DATABASE_URL=postgresql://user:password@hostname:port/database

# Server Configuration
PORT=5000
NODE_ENV=production

# Add any other environment variables your application needs below

# Note: In production, make sure to:
# 1. Use a secure database password
# 2. Set NODE_ENV to "production"
# 3. Configure proper PostgreSQL connection URL
# 4. Never commit the actual .env file to version control